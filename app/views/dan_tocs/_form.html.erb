<%= form_with(model: dan_toc, local: true) do |form| %>
  <% if dan_toc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dan_toc.errors.count, "error") %> prohibited this dan_toc from being saved:</h2>

      <ul>
        <% dan_toc.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :enabled %>
    <%= form.check_box :enabled %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
