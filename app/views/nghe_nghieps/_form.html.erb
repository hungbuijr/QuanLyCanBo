<%= form_with(model: nghe_nghiep, local: true) do |form| %>
  <% if nghe_nghiep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nghe_nghiep.errors.count, "error") %> prohibited this nghe_nghiep from being saved:</h2>

      <ul>
        <% nghe_nghiep.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
