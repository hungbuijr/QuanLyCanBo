<p id="notice"><%= notice %></p>

<h1>Chuc Vus</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Enabled</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @chuc_vus.each do |chuc_vu| %>
      <tr>
        <td><%= chuc_vu.name %></td>
        <td><%= chuc_vu.description %></td>
        <td><%= chuc_vu.enabled %></td>
        <td><%= link_to 'Show', chuc_vu %></td>
        <td><%= link_to 'Edit', edit_chuc_vu_path(chuc_vu) %></td>
        <td><%= link_to 'Destroy', chuc_vu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Chuc Vu', new_chuc_vu_path %>
