<%= form_with(model: ton_giao, local: true) do |form| %>
  <% if ton_giao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ton_giao.errors.count, "error") %> prohibited this ton_giao from being saved:</h2>

      <ul>
        <% ton_giao.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :enabled %>
    <%= form.check_box :enabled %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
