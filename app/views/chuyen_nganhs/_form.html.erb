<%= form_with(model: chuyen_nganh, local: true) do |form| %>
  <% if chuyen_nganh.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chuyen_nganh.errors.count, "error") %> prohibited this chuyen_nganh from being saved:</h2>

      <ul>
        <% chuyen_nganh.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :enabled %>
    <%= form.check_box :enabled %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
